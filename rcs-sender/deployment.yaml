apiVersion: apps/v1
kind: Deployment
metadata:
  name: rcs-sender
  namespace: rcs-wholesale
  labels:
    app: rcs-sender
    component: messaging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rcs-sender
  template:
    metadata:
      labels:
        app: rcs-sender
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8084"
    spec:
      serviceAccountName: rcs-wholesale-sa
      containers:
        - name: rcs-sender
          image: 615993872885.dkr.ecr.ap-northeast-2.amazonaws.com/rcs-wholesale-rcs-sender:2fc30bea52bccaa12faca3fdcc395d22d5f6c453
          ports:
            - containerPort: 8084
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: rcs-wholesale-db-secret
                  key: host
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: rcs-wholesale-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rcs-wholesale-db-secret
                  key: password
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: rcs-wholesale-redis-secret
                  key: host
            - name: REDIS_PORT
              value: "6380"
            - name: REDIS_SSL_ENABLED
              value: "true"
            - name: KAFKA_TOPIC_AUTO_CREATE
              value: "false"
            - name: MANAGEMENT_HEALTH_REDIS_ENABLED
              value: "false"
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: rcs-wholesale-config
                  key: kafka.brokers
            - name: KAFKA_SECURITY_PROTOCOL
              value: "SASL_SSL"
            - name: KAFKA_SASL_MECHANISM
              value: "AWS_MSK_IAM"
            - name: KAFKA_SASL_CALLBACK_HANDLER
              value: "software.amazon.msk.auth.iam.IAMClientCallbackHandler"
            - name: KAFKA_SASL_JAAS_CONFIG
              value: "software.amazon.msk.auth.iam.IAMLoginModule required;"
            - name: KAFKA_CONSUMER_GROUP
              value: "rcs-wholesale-sender"
            - name: MAAP_CORE_URL
              valueFrom:
                configMapKeyRef:
                  name: rcs-wholesale-config
                  key: maap.core.url
            - name: MAAP_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: rcs-maap-secret
                  key: client-id
            - name: MAAP_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: rcs-maap-secret
                  key: client-secret
            - name: AWS_REGION
              value: "ap-northeast-2"
            - name: DYNAMODB_TABLE_PREFIX
              value: "rcs_"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: rcs-wholesale-jwt-secret
                  key: secret
            - name: DEPLOY_ENV
              valueFrom:
                configMapKeyRef:
                  name: rcs-wholesale-config
                  key: deploy.env
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 120
            periodSeconds: 20
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: rcs-sender
  namespace: rcs-wholesale
spec:
  selector:
    app: rcs-sender
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8084
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rcs-sender-hpa
  namespace: rcs-wholesale
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rcs-sender
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
